{
  "name": "hme",
  "version": "1.0.0",
  "description": "HME helper apps",
  "main": "index.js",
  "scripts": {
    "start-client": "cd client && npm start",
    "start-server": "cd server && npm start",
    "git-merge-push": "git checkout main && git merge development && git commit -m \\\"Merging main with dev branch\\\" && git push && git checkout development"
  },
  "author": "Mitchell Smith",
  "license": "ISC"
}
